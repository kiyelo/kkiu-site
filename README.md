# Kkiu website

Product, privacy, terms, and account-deletion pages for Kkiu Todo.

- Korean: `/`, `/privacy/`, `/terms/`, `/delete-account/`
- English: `/en/`, `/en/privacy/`, `/en/terms/`, `/en/delete-account/`

Pushes to `main` deploy the static export to GitHub Pages. The custom domain is
`kkiu.3dayweekendlab.com`.
